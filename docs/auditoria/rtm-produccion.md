# RTM Backend – Módulo Producción

Fuente URS auditada: `docs/auditoria/requisitos-erp.md` (Req IDs PROD-001 a PROD-005).

| Req ID | Implementación encontrada | Archivos relevantes | Evidencia | Estado |
|---|---|---|---|---|
| PROD-001 | **Creación de órdenes de producción** mediante `POST /api/produccion/ordenes`. La creación pasa por `crearOrden(...)` y `guardarConValidacionStock(...)`, persiste `OrdenProduccion`, genera código de orden, asigna fecha de inicio, clona etapas de plantilla y dispara reserva de insumos. | `produccion/controller/OrdenProduccionController.java`<br>`produccion/service/OrdenProduccionServiceImpl.java`<br>`produccion/repository/OrdenProduccionRepository.java`<br>`produccion/model/OrdenProduccion.java` | **Backend Controller:** endpoint `POST /api/produccion/ordenes`.<br>**Backend Service:** `crearOrden`, `guardarConValidacionStock`, `clonarEtapasParaOrden`, `reservarInsumosParaOP`.<br>**Backend Repository:** `OrdenProduccionRepository` (persistencia y consulta de OP).<br>**Backend Entity:** `OrdenProduccion` con estado, cantidades, responsable y trazas de cierre.<br>**Validaciones de negocio:** exige plantilla de etapas para producto y fórmula BOM aprobada/activa antes de crear. | IMPLEMENTADO_BACKEND |
| PROD-002 | **Validación de disponibilidad de insumos** al crear/actualizar OP, incluyendo cálculo FEFO por insumo, faltantes, máximas unidades producibles y bloqueo por stock insuficiente. También considera modo de control de inventario (`CONTROL_STOCK` vs `SIN_CONTROL_STOCK`). | `produccion/service/OrdenProduccionServiceImpl.java`<br>`produccion/service/DisponibilidadInsumoService.java`<br>`bom/repository/FormulaProductoRepository.java`<br>`inventario/repository/LoteProductoRepository.java` | **Backend Service:** `guardarConValidacionStock` evalúa cada `DetalleFormula`, calcula requerido vs disponible y retorna `ResultadoValidacionOrdenDTO` inválido si faltan insumos.<br>**Integración BOM:** lee fórmula activa/aprobada por producto (`FormulaProductoRepository`).<br>**Integración Inventarios:** `DisponibilidadInsumoService.calcularDisponibilidad(...)` consulta lotes FEFO (`LoteProductoRepository.findFefoDisponibles`) y calcula stock libre/ faltante por almacén permitido.<br>**Validaciones de negocio:** rechaza creación sin fórmula aprobada y cuando el stock controlado no cubre requerimientos. | IMPLEMENTADO_BACKEND |
| PROD-003 | **Registro de consumo de materias primas** en la ejecución/cierre de OP: consumo por etapa (`SALIDA_PRODUCCION`), consumo real en cierre total y devoluciones por cierre parcial; además existen endpoints para consultar consumos/movimientos por orden y etapa. | `produccion/controller/OrdenProduccionController.java`<br>`produccion/service/OrdenProduccionServiceImpl.java`<br>`inventario/service/MovimientoInventarioService.java` (invocado)<br>`inventario/repository/MovimientoInventarioRepository.java` (consultado) | **Backend Controller:** `GET /api/produccion/ordenes/{ordenId}/etapas/{etapaId}/consumos`, `GET .../movimientos` y `GET /{id}/movimientos`.<br>**Backend Service:** `registrarConsumoDeInsumos`, `registrarConsumoRealPorCierreTotal`, `registrarDevolucionInsumosPorCierreParcial`, y llamadas a `movimientoInventarioService.consumirInsumosPorOrden(...)`.<br>**Integración Inventarios:** persistencia/lectura de `MovimientoInventario` por clasificación (`SALIDA_PRODUCCION`, `TRANSFERENCIA_INTERNA_PRODUCCION`).<br>**Validaciones de negocio:** evita consumos duplicados por etapa y ajusta consumos contra alistados/reservas. | IMPLEMENTADO_BACKEND |
| PROD-004 | **Registro y gestión de etapas de producción**: creación/listado de etapas, clonación desde plantilla al crear OP, inicio/finalización por workflow, registro de fechas/usuario y controles de secuencia operativa (una etapa activa por orden). | `produccion/controller/OrdenProduccionController.java`<br>`produccion/controller/EtapaProduccionController.java`<br>`produccion/service/OrdenProduccionServiceImpl.java`<br>`produccion/repository/EtapaProduccionRepository.java`<br>`produccion/model/EtapaProduccion.java` | **Backend Controller:** endpoints de etapas (`/api/produccion/etapas`, `/api/produccion/ordenes/{id}/etapas`, iniciar/finalizar etapa).<br>**Backend Service:** `clonarEtapasParaOrden`, `iniciarEtapa`, `finalizarEtapa`, `recalcularEstadoOrden`.<br>**Backend Repository:** `EtapaProduccionRepository` con consultas por orden, activas e idempotencia.<br>**Backend Entity:** `EtapaProduccion` registra estado, secuencia, fechas y usuario.<br>**Validaciones de negocio:** no permite iniciar etapa no pendiente, prohíbe múltiples etapas activas y valida checklist completo antes de finalizar etapa. | IMPLEMENTADO_BACKEND |
| PROD-005 | **Cierre de orden de producción** mediante `POST /api/produccion/ordenes/{id}/cierres`, con persistencia de cierres, actualización de acumulados/estado final de OP, validaciones de reglas de cierre y generación de movimiento de entrada de producto terminado. | `produccion/controller/OrdenProduccionController.java`<br>`produccion/service/OrdenProduccionServiceImpl.java`<br>`produccion/repository/CierreProduccionRepository.java`<br>`produccion/model/CierreProduccion.java`<br>`produccion/model/OrdenProduccion.java` | **Backend Controller:** `POST /{id}/cierres` y `GET /{id}/cierres`.<br>**Backend Service:** `registrarCierre` aplica reglas para `TOTAL`/`PARCIAL`, valida estado de OP, etapa iniciada/finalizada, fechas de fabricación/vencimiento, reservas pendientes y confirmaciones; luego guarda `CierreProduccion` y recalcula estado de orden (`FINALIZADA`/`CERRADA_INCOMPLETA`).<br>**Backend Repository/Entity:** `CierreProduccionRepository` + entidad `CierreProduccion` para histórico de cierres.<br>**Integración Inventarios:** en cierre registra entrada de PT/PS en inventario y libera reservas cuando aplica. | IMPLEMENTADO_BACKEND |

## Observaciones generales backend

- El backend de Producción presenta cobertura funcional para los cinco requisitos auditados del URS (PROD-001 a PROD-005).
- La trazabilidad se apoya fuertemente en integración con **BOM** (fórmula activa/aprobada) e **Inventarios** (FEFO, reservas, movimientos y lotes).
- No se evidenció en esta auditoría ausencia total de endpoints o servicios para los requisitos listados; por eso no se clasificó ningún requisito como `PARCIAL` o `NO_IMPLEMENTADO` en alcance backend.
