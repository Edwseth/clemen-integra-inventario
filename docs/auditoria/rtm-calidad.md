# Matriz de Trazabilidad (RTM) — Calidad

Fuente de verdad de requisitos: `docs/auditoria/requisitos-erp.md`.

Alcance de esta auditoría: análisis técnico del backend en `src/main/java/com/willyes/clemenintegra/calidad` y su integración con Inventarios/Producción (`src/main/java/com/willyes/clemenintegra/inventario`, cuando aplica).

Criterio aplicado en esta revisión (solo backend):
- **IMPLEMENTADO_BACKEND**: existe endpoint + lógica + persistencia.
- **PARCIAL**: existe implementación incompleta en backend.
- **NO_IMPLEMENTADO**: no existe implementación en backend.

| Req ID | Implementación encontrada | Archivos relevantes | Evidencia | Estado |
|---|---|---|---|---|
| QC-001 | Se registra evaluación de calidad por lote mediante `POST /api/calidad/evaluaciones` (multipart), con persistencia de resultado, tipo, observaciones, evaluador y fecha; incluye listados/detalle para consulta y consolidado por lote. Validaciones de negocio: solo permite crear/actualizar si lote está en `EN_CUARENTENA`/`RETENIDO`, y valida rol del evaluador según tipo de evaluación (físico o químico-microbiológico). Integración con Inventarios: la evaluación referencia `LoteProducto` y usa repositorio de lotes para validar estado/almacén de cuarentena. Integración con Producción: indirecta por `LoteProducto` ligado a `OrdenProduccion`. | `calidad/controller/EvaluacionCalidadController.java`; `calidad/service/EvaluacionCalidadServiceImpl.java`; `calidad/repository/EvaluacionCalidadRepository.java`; `calidad/model/EvaluacionCalidad.java`; `inventario/model/LoteProducto.java` | Backend Controller; Backend Service; Backend Repository; Backend Entity; Validaciones de negocio de estado/rol | IMPLEMENTADO_BACKEND |
| QC-002 | El backend permite adjuntar documentos/soportes de análisis en dos niveles: (1) adjuntos por evaluación (`archivos` multipart en evaluación), guardados en disco (`uploads/evaluaciones`) y persistidos como `archivosAdjuntos`; (2) gestión documental de calidad (`/api/calidad/documentos`, versiones y descarga) con asociación opcional a lote. Validaciones: sanitización de nombre de archivo y bloqueo de versionado sobre documento obsoleto. Integración con Inventarios: documentos pueden asociarse a `loteId`; evaluaciones siempre se asocian a lote. | `calidad/controller/EvaluacionCalidadController.java`; `calidad/service/EvaluacionCalidadServiceImpl.java`; `calidad/model/ArchivoEvaluacion.java`; `calidad/controller/DocumentoCalidadController.java`; `calidad/service/DocumentoCalidadServiceImpl.java`; `calidad/repository/DocumentoCalidadRepository.java`; `calidad/repository/DocumentoCalidadVersionRepository.java` | Backend Controller; Backend Service; Backend Repository; Backend Entity | IMPLEMENTADO_BACKEND |
| QC-003 | Se implementa aprobación/rechazo de lotes mediante: (a) liberación por calidad `PUT /api/calidad/lotes/{loteId}/liberar` (aprueba) y (b) rechazo/resultado no conforme desde evaluación (`ResultadoEvaluacion.NO_CONFORME`) que dispara no conformidad y retención del lote. Reglas de liberación validan evaluaciones requeridas, soportes adjuntos, ausencia de NC activa, ausencia de retenciones y condiciones de uso activas. Integración con Inventarios: actualización de `EstadoLote`, `fechaLiberacion`, usuario liberador y movimiento de inventario de cuarentena a almacén destino. Integración con Producción: movimientos de liberación preservan vínculo `ordenProduccion`. | `calidad/controller/LoteCalidadController.java`; `inventario/service/LoteProductoServiceImpl.java`; `calidad/service/EvaluacionCalidadServiceImpl.java`; `calidad/service/RetencionLoteServiceImpl.java`; `calidad/service/NoConformidadServiceImpl.java`; `inventario/repository/LoteProductoRepository.java` | Backend Controller; Backend Service; Backend Repository; Backend Entity; Validaciones de negocio para liberar/rechazar | IMPLEMENTADO_BACKEND |
| QC-004 | Existe bloqueo de lotes no liberados en operaciones de inventario mediante `LoteCalidadValidator.validarLoteUtilizable`: impide uso de lotes en `EN_CUARENTENA`, `RETENIDO`, `RECHAZADO`, `VENCIDO`, y también bloquea por NC activa o condición de uso activa. Además, las evaluaciones de calidad solo se registran/actualizan si el lote permanece en cuarentena/retención, evitando inconsistencias de flujo. Integración con Inventarios: validador aplicado en servicios de movimientos/consumos; bloqueo explícito por código de error de calidad. | `inventario/service/LoteCalidadValidator.java`; `inventario/service/MovimientoInventarioServiceImpl.java`; `calidad/service/EvaluacionCalidadServiceImpl.java`; `inventario/model/LoteProducto.java` | Backend Service; Validaciones de negocio; Integración Inventarios (estados de lote) | IMPLEMENTADO_BACKEND |
| QC-005 | Se registra trazabilidad de evaluaciones y del estado de calidad por lote: endpoint de auditoría `GET /api/calidad/auditoria-lote/{loteId}` (y PDF), además de trazabilidad desde inventarios (`/api/inventarios/trazabilidad/lote/{loteId}`). La respuesta de auditoría consolida evaluaciones, adjuntos, resultados micro, incidentes (NC/CAPA), retenciones, condiciones de uso y movimientos de inventario (incluye código de orden de producción cuando existe). También se registra bitácora en cambios de estado (liberación/reapertura/retención). | `calidad/controller/CalidadAuditoriaController.java`; `inventario/controller/TrazabilidadLoteController.java`; `calidad/service/AuditoriaLoteServiceImpl.java`; `inventario/service/LoteProductoServiceImpl.java`; `inventario/repository/MovimientoInventarioRepository.java`; `calidad/repository/EvaluacionCalidadRepository.java` | Backend Controller; Backend Service; Backend Repository; Evidencia de trazabilidad end-to-end por lote | IMPLEMENTADO_BACKEND |

## Resultado de Auditoría Backend

Requisitos evaluados: 5  
IMPLEMENTADO_BACKEND: 5  
PARCIAL: 0  
NO_IMPLEMENTADO: 0

Cobertura backend del módulo Calidad (QC-001 a QC-005): **100%**.
